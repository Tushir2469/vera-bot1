# vera-bot1
FastAPI bot for the magicpin Vera Challenge — implements the 5-endpoint judge contract (/v1/context, /v1/tick, /v1/reply, /v1/healthz, /v1/metadata) with a Claude-based composer over the 4-context framework (category, merchant, trigger, customer).
